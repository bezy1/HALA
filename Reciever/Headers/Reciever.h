#ifndef _RECIEVER_H_
#define
int Recieve(char * message); // This Where We Reiceve Messages, Even Loop
int Checker(char * message); // This is Where We Check User Input 
int Setup(int * sockfd); // This Where We Setup The First Connection
int Kill(); // This is used if i have fucked up
FILE * Start(FILE * fp); // This is used When Starting the Program
FILE * Close(FILE * fp): // This is used if we Want to close The program
#endif
